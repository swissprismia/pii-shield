name: Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  # Build Windows release binary
  build-windows:
    name: Build (Windows x64)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: "npm"

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: npm ci

      - name: Install Python sidecar dependencies
        working-directory: sidecar
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Download spaCy English model
        run: python -m spacy download en_core_web_lg

      - name: Build Python sidecar
        working-directory: sidecar
        run: python build_sidecar.py

      - name: Build Tauri app
        run: npm run tauri build
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v7
        with:
          name: windows-release
          path: |
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/bundle/msi/*.msi

  # Build macOS release binary
  build-macos:
    name: Build (macOS Universal)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust stable + macOS targets
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: "npm"

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: npm ci

      - name: Install Python sidecar dependencies
        working-directory: sidecar
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Download spaCy English model
        run: python -m spacy download en_core_web_lg

      - name: Build Python sidecar (arm64)
        working-directory: sidecar
        run: python build_sidecar.py

      - name: Build Python sidecar (x86_64 via Rosetta)
        working-directory: sidecar
        run: |
          arch -x86_64 python3 -m venv venv-x86_64
          arch -x86_64 venv-x86_64/bin/pip install -r requirements.txt pyinstaller
          arch -x86_64 venv-x86_64/bin/python -m spacy download en_core_web_lg
          arch -x86_64 venv-x86_64/bin/python build_sidecar.py
        continue-on-error: true

      - name: Create universal sidecar binary
        run: |
          ARM64=src-tauri/binaries/presidio-sidecar-aarch64-apple-darwin
          X86=src-tauri/binaries/presidio-sidecar-x86_64-apple-darwin
          UNIVERSAL=src-tauri/binaries/presidio-sidecar-universal-apple-darwin
          if [ -f "$X86" ] && lipo -archs "$X86" 2>/dev/null | grep -q x86_64; then
            lipo -create -output "$UNIVERSAL" "$ARM64" "$X86"
            echo "Created real universal binary ($(lipo -archs "$UNIVERSAL"))"
          else
            echo "x86_64 binary missing or not a valid x86_64 Mach-O, falling back to arm64 copy"
            cp "$ARM64" "$UNIVERSAL"
            cp "$ARM64" "$X86"
          fi

      - name: Build Tauri app (universal)
        run: npm run tauri build -- --target universal-apple-darwin --no-sign
        env:
          APPLE_CERTIFICATE: ""
          APPLE_SIGNING_IDENTITY: ""
          TAURI_SIGNING_PRIVATE_KEY: ""
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v7
        with:
          name: macos-release
          path: src-tauri/target/universal-apple-darwin/release/bundle/dmg/*.dmg

  # Build Linux release binary
  build-linux:
    name: Build (Linux x64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install system dependencies (Linux)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libxdo-dev \
            libxtst-dev \
            libx11-dev

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: "npm"

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: npm ci

      - name: Install Python sidecar dependencies
        working-directory: sidecar
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Download spaCy English model
        run: python -m spacy download en_core_web_lg

      - name: Build Python sidecar
        working-directory: sidecar
        run: python build_sidecar.py

      - name: Build Tauri app
        run: npm run tauri build -- --bundles appimage,deb

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v7
        with:
          name: linux-release
          path: |
            src-tauri/target/release/bundle/appimage/*.AppImage
            src-tauri/target/release/bundle/deb/*.deb

  # Create GitHub Release
  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [build-windows, build-macos, build-linux]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Download Windows artifacts
        uses: actions/download-artifact@v8
        with:
          name: windows-release
          path: release-assets/windows/

      - name: Download macOS artifacts
        uses: actions/download-artifact@v8
        with:
          name: macos-release
          path: release-assets/macos/

      - name: Download Linux artifacts
        uses: actions/download-artifact@v8
        with:
          name: linux-release
          path: release-assets/linux/

      - name: Generate changelog from conventional commits
        id: changelog
        uses: orhun/git-cliff-action@v4
        with:
          config: cliff.toml
          args: --latest --strip header
        continue-on-error: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: PII Shield ${{ github.ref_name }}
          body: |
            ## What's Changed

            ${{ steps.changelog.outputs.content || 'See commit history for changes.' }}

            ## Installation

            | Platform | File |
            |----------|------|
            | Windows | `.exe` installer (NSIS) |
            | macOS (Apple Silicon + Intel) | `.dmg` disk image |
            | Linux (x64) | `.AppImage` and `.deb` packages |

            See the [README](https://github.com/${{ github.repository }}/blob/main/README.md#installation) for full installation instructions.

            ---
            > All PII processing happens **100% locally** - no data leaves your machine.
          files: |
            release-assets/windows/**
            release-assets/macos/**
            release-assets/linux/**
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
